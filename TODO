* ReplayGain support on videos if possible.

* Improve webradios as commented in the tests.

* Random with weight on notation.

* Count playback start and playback stop in db. The difference between those is
  the number of times skipped.

* Work on webui from use cases (albumplayback, etc).

* Signals to the webui exploiting the fact that XmlHttpRequest does not have a
  timeout.

* Have intelligent playlists (all library, etc)

* debian package should make it easier to connect to Xorg

* commonalize logrotate files

* devscripts dir

* fix ratio using avi header

* build testing library once and for all tests for speedups
